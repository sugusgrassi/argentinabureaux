import { makeStyles } from '@material-ui/core/styles';

// This is how to do styles in material-ui
// cb that provides instantly an object 
export default makeStyles(() => ({
    ul: {
        justifyContent: 'space-around'
    }
}))